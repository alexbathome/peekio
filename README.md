# peekio

peekio is a µGo package that provides an `io.Reader` implementation over a `Peeker`. 

where a `Peeker` is a `Peek(int) ([]byte, error)` interface.

## docs

please see https://pkg.go.dev/github.com/alexbathome/peekio for documentation :)
